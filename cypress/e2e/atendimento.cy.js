describe('Central de Atendimento', () => { it('permite abrir um chamado', () => { cy.visit('/'); cy.get('input[type=email]').clear().type('ana@elo.com'); cy.get('input[type=password]').clear().type('123456'); cy.contains('Entrar na plataforma').click(); cy.contains('Novo chamado').click(); cy.get('input[name=titulo]').type('Problema E2E'); cy.get('textarea[name=descricao]').type('Chamado criado pela automação'); cy.contains('Criar chamado').click(); cy.contains('Problema E2E').should('be.visible'); }); });
